{
  "scripts": {
    "postinstall": "cd resources && npm install",
    "dev": "cd resources && npm run dev",
    "build": "cd resources && npm run build"
  },
  "devDependencies": {
    "@shopify/prettier-plugin-liquid": "^1.5.0",
    "prettier": "^3.3.2",
    "prettier-plugin-tailwindcss": "^0.6.5"
  }
}
